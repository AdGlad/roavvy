name: region_lookup
description: Offline GPS coordinate to ISO 3166-2 region code resolution. Zero network dependency.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  test: ^1.25.0
