{
  "name": "firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "T6 — Firestore security rules tests using Firebase Rules Test SDK",
  "scripts": {
    "test": "jest --testEnvironment=node",
    "test:watch": "jest --testEnvironment=node --watch"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase-admin": "^12.0.0",
    "jest": "^29.0.0"
  },
  "jest": {
    "testMatch": ["**/*.test.js"]
  }
}
